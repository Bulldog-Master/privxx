project_id = "qgzoqsgfqmtcpgfgtfms"

[functions.turnstile-config]
verify_jwt = false

[functions.verify-turnstile]
verify_jwt = false

# Auth-related functions perform their own JWT validation (when required)
# and must be callable from unauthenticated contexts (e.g., passkey sign-in).
[functions.passkey-auth]
verify_jwt = false

[functions.totp-auth]
verify_jwt = false

[functions.security-notify]
verify_jwt = true

# Avatar processing handles its own JWT verification from auth header
[functions.process-avatar]
verify_jwt = false

# Referral processing uses service role, callable from signup flow
[functions.process-referral]
verify_jwt = false
