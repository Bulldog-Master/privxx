package main

import (
	"encoding/json"
	"net/http"
)

// Minimal JSON helper for Phase 7 handlers (bridge stays plaintext-free; no crypto here).
func writeJSON(w http.ResponseWriter, code int, v any) {
	w.Header().Set("Content-Type", "application/json")
	w.WriteHeader(code)
	_ = json.NewEncoder(w).Encode(v)
}

// Phase 7 (temporary compile-safe handlers).
// Next step will replace bodies with authenticated forwarders to backend.
// NOTE: handleBrowsePreview is implemented in message_browse_routes.go — do not duplicate it here.

func handleMessageSend(w http.ResponseWriter, r *http.Request) {
	writeJSON(w, http.StatusNotImplemented, map[string]any{
		"error": "not_implemented",
		"code":  "phase7_pending_forwarder",
	})
}

func handleMessageInbox(w http.ResponseWriter, r *http.Request) {
	writeJSON(w, http.StatusNotImplemented, map[string]any{
		"error": "not_implemented",
		"code":  "phase7_pending_forwarder",
	})
}
